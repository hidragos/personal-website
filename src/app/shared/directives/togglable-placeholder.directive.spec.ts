import { TogglablePlaceholderDirective } from './togglable-placeholder.directive';

describe('TogglablePlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglablePlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
